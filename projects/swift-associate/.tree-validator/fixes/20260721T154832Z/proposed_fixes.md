# Đề xuất sửa lỗi (chờ duyệt)

## `BROKEN_REFERENCE` — categories / `STATE_DATA_FLOW`
- Tham chiếu gãy: `DECLARATIVE_UI` (cột `subject_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — categories / `STATE_DATA_FLOW`
- Tham chiếu gãy: `ASE` (cột `subject_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — categories / `UI_BUILDING_BLOCKS`
- Tham chiếu gãy: `HCC` (cột `subject_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — topics / `DIGITAL_INTERACTION`
- Tham chiếu gãy: `DIGITAL_LITERACY` (cột `category_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — concepts / `USER_CENTERED_DESIGN`
- Tham chiếu gãy: `UI_UX_PROCESS` (cột `topic_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — concepts / `DIGITAL_IDENTITY`
- Tham chiếu gãy: `DATA_PRIVACY_USER` (cột `topic_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — concepts / `STATE_PROPERTY_WRAPPER`
- Tham chiếu gãy: `LOCAL_VIEW_STATE` (cột `topic_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-DEV-ENV`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-XCODE-FILES`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-XCODE-SWIFT-FILE`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-XCODE-ASSET-FILE`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-XCODE-ASSETS`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ASSETS-RECOGNIZE`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ASSETS-IMPORT`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ASSETS-USE`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-XCODE-UI`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-XCODE-NAVIGATOR`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-XCODE-INSPECTOR`
- Tham chiếu gãy: `PROJECT_ASSETS_MANAGEMENT` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-FUNCTIONS`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-FUNCTIONS`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-FUNC-WRITE`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-FUNC-CALL`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-FUNC-LABELS`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-FUNC-PARAMS`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-FUNC-RETURNS`
- Tham chiếu gãy: `FUNCTIONS_METHODS` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-OPERATORS`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-OPERATORS`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-OP-ARITHMETIC`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-OP-LOGICAL`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-CUSTOM-TYPES`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-STRUCTS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-STRUCT-DECL_PROP`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-STRUCT-INIT`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-STRUCT-METHODS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-STRUCT-INST_CREATE`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-STRUCT-INST_USE`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-COLLECTIONS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-ARRAYS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-DECL`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-INIT`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-INDEX`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-MOD_INDEX`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-PROPS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-ARRAY-METHODS`
- Tham chiếu gãy: `DATA_TYPES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `ULO-PROG-STATE`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-VARS`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-VAR-DIFF`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-VAR-INFER`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-VAR-EXPLICIT`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `CIO-SWIFT-NAMING`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-NAME-CAMEL`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.

## `BROKEN_REFERENCE` — learning_objectives / `SIO-NAME-RULES`
- Tham chiếu gãy: `VARIABLES` (cột `concept_codes`)
- Không tìm thấy code nào đủ giống — cần bổ sung thủ công.
