# Báo cáo kiểm tra Knowledge Tree

- **Thời gian chạy:** 2026-07-21T14:14:10.302766+00:00
- **Kết quả:** ❌ FAIL
- **Tổng số issue:** 261 (1 lỗi, 260 cảnh báo)

## Số lượng node theo tầng

| Tầng | Số node |
|---|---|
| fields | 6 |
| subjects | 22 |
| categories | 71 |
| topics | 165 |
| concepts | 237 |
| learning_objectives | 0 |

## Tổng hợp theo rule

| Rule | Mô tả | Số lượng |
|---|---|---|
| `ORPHAN_NODE` | Node không được node nào ở tầng dưới tham chiếu tới | 260 |
| `MISSING_FILE` | Không tìm thấy file dữ liệu | 1 |

## ❌ Lỗi (ERROR) — cần sửa

### `MISSING_FILE` (1) — Không tìm thấy file dữ liệu

| Tầng | Code | Cột | Chi tiết |
|---|---|---|---|
| learning_objectives | `-` | - | Không tìm thấy file learning-objectives-ltasw.tsv |

## ⚠️ Cảnh báo (WARNING)

### `ORPHAN_NODE` (260) — Node không được node nào ở tầng dưới tham chiếu tới

| Tầng | Code | Cột | Chi tiết |
|---|---|---|---|
| categories | `AI_APPLICATIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| categories | `BINARY_SYSTEM` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| categories | `INTERACTION_DEVICES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `ADAPTIVE_LAYOUT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `AGILE_SCRUM` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `DATA_VIS_TOOLS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `DECLARATIVE_UI_MODEL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `EDGE_FOG_CLOUD` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `EMBEDDED_SYSTEMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `ERROR_MESSAGES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `GIT_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `GRAPH_THEORY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `HASH_TABLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `HIERARCHICAL_NAVIGATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `HW_IO` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `IOT_DEVICES_SENSORS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `MODAL_PRESENTATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `RENDERING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `ROBOT_CONTROL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `ROBOT_KINEMATICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `TRANSACTION_MANAGEMENT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `TREES_HEAPS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| topics | `TREE_STRUCTURES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `2D_3D_TRANSFORMATIONS_MATH` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ABSTRACTION_LAYERS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ACCESS_MODIFIERS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ADJUSTMENT_LAYERS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `AFFORDANCES_SIGNIFIERS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `AGILE_PRINCIPLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `AI_BIAS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `AI_HISTORY_MILESTONES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `AI_VS_ML` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ALGORITHMIC_BIAS_SOCIETY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `API_DESIGN` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `API_INTEGRATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `APP_EXTENSION_MODEL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `APP_PROTOCOLS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ARDUINO_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ARITHMETIC_OPS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ARRAY_OPERATIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ASYNCHRONOUS_PROG` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BACKEND_FRAMEWORKS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BASIC_ELECTRONIC_COMPONENTS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BASIC_MECHANISMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BEHAVIORAL_PATTERNS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BIG_O_NOTATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BOOLEAN_OPERATIONS_3D` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BREAKPOINTS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `BUBBLE_INSERTION_SORT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CACHE_HIERARCHY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CAP_THEOREM` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CHART_TYPES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CIRCUIT_PRINCIPLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLASSES_OBJECTS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLASS_DEFINITION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLOUD_COMPUTING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLOUD_DEPLOYMENT_MODELS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLOUD_MODELS_IAAS_PAAS_SAAS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CLUSTERING_ALGORITHMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `COLLABORATIVE_PLATFORMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `COLOR_THEORY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `COMPARISON_LOGICAL_OPS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `COMPOSITION_PRINCIPLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CONDITIONAL_LOGIC` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `COPYRIGHT_CREATIVE_COMMONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CORE_DATA_ORM` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CPU_ARCHITECTURE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CREATIONAL_PATTERNS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CSS_SELECTORS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `CYBERBULLYING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATABASE_NORMALIZATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_BINDING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_CLEANING_TECHNIQUES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_DISTRIBUTIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_ENCODING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_EXPLORATION_EDA` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DATA_STRUCTURES_BASIC` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DEBUGGING_TECH` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DESIGN_PATTERNS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DEVELOPMENT_PROCESS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DEVICE_PROTECTION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIGITAL_ANALOG_IO` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIGITAL_CITIZENSHIP` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIGITAL_DIVIDE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIGITAL_FOOTPRINT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIGITAL_IDENTITY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DIVIDE_CONQUER` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DMA_CONCEPT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DNS_LOOKUP` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DOM_MANIPULATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DRAG_GESTURE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `DYNAMIC_PROGRAMMING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ENCAPSULATION_ABSTRACTION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ENCRYPTION_PROTOCOLS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ERROR_MESSAGES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `EVENT_BASED_PROGRAMMING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `EVENT_HANDLERS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `EXCEPTION_HANDLING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `EXPLICIT_ANIMATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FEEDBACK_AND_RESPONSE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FETCH_DECODE_EXECUTE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FILE_ALLOCATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FIRST_CLASS_FUNCTIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FOR_LOOP` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FRONTEND_FRAMEWORKS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FUNCTIONS_PROCEDURES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `FUNC_SYNTAX` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `GEOSPATIAL_SERVICES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `GESTURE_HANDLING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `GRAPHIC_DESIGN_PRINCIPLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `GREEDY_ALGORITHMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `GRID_LAYOUT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `HDD_VS_SSD` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `HTML_STRUCTURE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `HTTP_METHODS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IF_ELSE_STATEMENT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMAGE_COMPOSITING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMAGE_RETOUCHING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMAGE_WARPING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMMUTABILITY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMPLICIT_ANIMATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IMPLICIT_EXPLICIT_ANIMATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `INFORMATION_CREDIBILITY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `INHERITANCE_POLY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `INHERITANCE_SYNTAX` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IOT_PROTOCOLS_MQTT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IOT_SAFETY_RISKS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IOT_SECURITY_THREATS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IO_METHODS_POLLING_INTERRUPT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `IPV4_VS_IPV6` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ITERATION_LOOPS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `JSON_SERIALIZATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `KEYFRAME_ANIMATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `KEY_VALUE_PERSISTENCE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LAYER_BLEND_MODES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LAYER_MASKING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LAYOUT_COMPOSITION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LIGHTING_AND_SHADOWS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LIGHT_MOTION_SENSORS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LINEAR_BINARY_SEARCH` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LIST_OPERATIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LOCAL_NOTIFICATION_API` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LOCAL_VIEW_STATE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LOCATION_SERVICES_API` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LOGIC_ERRORS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LOGIC_GATES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `LONGPRESS_GESTURE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MALWARE_TYPES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MAPREDUCE_CONCEPT` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MAP_ANNOTATIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MAP_INTEGRATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MATERIAL_AND_TEXTURE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MEDIA_PICKER_API` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MEDIA_SERVICES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MEMORY_TYPES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MESH_EXTRUSION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `METHOD_OVERRIDING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MOTOR_CONTROL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `MVVM_PATTERN` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NETIQUETTE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NETWORK_PROTOCOLS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NETWORK_SECURITY` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NETWORK_TOPOLOGIES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NEURAL_NETWORKS_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `NOTIFICATION_SERVICES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OBJECT_INSTANTIATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OBJECT_PROPERTIES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OBSERVABLE_MODEL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ONDEVICE_PERSISTENCE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OOP` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OPERATORS_EXPRESSIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OSI_MODEL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OS_CONCEPTS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `OS_FILE_SYSTEMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PASSWORD_STRENGTH` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PERSONAS_AND_SCENARIOS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PHISHING_IDENTIFICATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PHYSICAL_MEDIA` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `POLYGON_MESH` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PRIMITIVE_TYPE_DECLARATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PROBABILITY_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PROBLEM_DECOMPOSITION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PROCESS_VS_THREAD` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PROTOTYPING` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PUBLISHER_SUBSCRIBER_MODEL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `PWM` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `QR_CODE_GENERATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `QUEUE_OPERATIONS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `RASPBERRY_PI_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `REFERENCE_TYPES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `REFERENCE_TYPE_DECLARATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `REGRESSION_CLASSIFICATION` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `REINFORCEMENT_LEARNING_BASICS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `RELATIONAL_VS_NONRELATIONAL` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `RENDERING_PIPELINE` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `REST_PRINCIPLES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `RETURN_VALUES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `ROUTERS_SWITCHES` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `RUNTIME_ERRORS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| concepts | `SCHEDULING_ALGORITHMS` | - | Không có node nào ở tầng dưới tham chiếu tới node này. |
| ... | ... | ... | (+60 dòng nữa) |
